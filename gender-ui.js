/* Ommi Food: normalize chef gender and adapt visible kitchen wording. */
function isMaleChefGender(g){const v=String(g??'').trim().toLowerCase();return ['m','male','man','homme','masculin','ذكر'].includes(v)}
prefix=function(g){return isMaleChefGender(g)?'عمّي':'أمّي'};
function applyChefGenderUi(g){const male=isMaleChefGender(g);const topbar=document.querySelector('#buildKitchen .topbar span');if(topbar)topbar.textContent=male?'أنت الآن داخل Ommi Food':'أنتِ الآن داخل Ommi Food';const firstStep=document.querySelector('#buildKitchen .builder-step:nth-of-type(2) p');if(firstStep)firstStep.textContent=male?'اختر نوع مطبخك':'اختاري نوع مطبخك';const addStep=[...document.querySelectorAll('#buildKitchen .builder-step')].find(x=>x.querySelector('h2')?.textContent.includes('أطباقك'));if(addStep){const p=addStep.querySelector('p');if(p)p.textContent=male?'أضف أطباقك واحدًا واحدًا. الصورة اختيارية ويمكن إضافتها لاحقًا.':'أضيفي أطباقك واحدًا واحدًا. الصورة اختيارية ويمكن إضافتها لاحقًا.'}const daysStep=[...document.querySelectorAll('#buildKitchen .builder-step')].find(x=>x.querySelector('h2')?.textContent.includes('متى'));if(daysStep){const p=daysStep.querySelector('p');if(p)p.textContent=male?'اختر الأيام التي تستقبل فيها الطلبات.':'اختاري الأيام التي تستقبلين فيها الطلبات.'}}
const _openBuilder=openBuilder;openBuilder=function(){_openBuilder();applyChefGenderUi(currentChefGender);document.getElementById('builderKitchenName').textContent=`مطبخ ${prefix(currentChefGender)} ${currentChefName}`;updatePreview()};