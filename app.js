const SUPABASE_URL='https://qgblrockjswicegfldzm.supabase.co';
const SUPABASE_KEY='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXAiLCJyZWYiOiJxZ2Jscm9ja2pzd2ljZWdmbGR6bSIsInJvbGUiOiJhbm9uIiwiaWF0IjoxNzgyMjI4NzM4LCJleHAiOjIwOTc4MDQ3Mzh9.zQASySR7h7iXnqqPahzPQNnIQnCSQ4Tpxk1ujtf59U4';
const db=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);
const citySelect=document.getElementById('citySelect'),comingModal=document.getElementById('comingModal'),orderModal=document.getElementById('orderModal'),joinModal=document.getElementById('joinModal'),toast=document.getElementById('toast');let chefsCache=[],activeChef=null,activeDish=null,joinGender='f';
function prefix(g){return g==='m'?'عمّي':'أمّي'}function cleanName(v){return(v||'').replace(/^(أمّي|أمي|امي|عمّي|عمي|Ommi|Oncle)\s*/i,'').trim()}function showScreen(id){document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));document.getElementById(id).classList.add('active');window.scrollTo(0,0)}function showToast(text){toast.textContent=text;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2600)}function openModal(el){el.classList.add('open');el.setAttribute('aria-hidden','false')}function closeModal(el){el.classList.remove('open');el.setAttribute('aria-hidden','true')}
function clearJoinErrors(){joinModal.querySelectorAll('.field-error').forEach(el=>el.remove());joinModal.querySelectorAll('.field.invalid').forEach(el=>el.classList.remove('invalid'));document.getElementById('joinConsent')?.closest('label')?.classList.remove('consent-invalid')}function joinFieldError(id,message){const field=document.getElementById(id);if(!field)return;field.classList.add('invalid');const error=document.createElement('div');error.className='field-error';error.textContent=message;field.insertAdjacentElement('afterend',error);field.scrollIntoView({behavior:'smooth',block:'center'})}
document.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',()=>closeModal(b.closest('.modal'))));document.querySelectorAll('.modal').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)closeModal(m)}));document.querySelectorAll('[data-back]').forEach(b=>b.addEventListener('click',()=>showScreen('home')));document.querySelectorAll('[data-open-join]').forEach(b=>b.addEventListener('click',()=>openModal(joinModal)));
async function loadChefs(){const grid=document.getElementById('chefGrid');grid.innerHTML='<div class="empty-state loading">جاري تحميل المطابخ...</div>';const{data,error}=await db.from('chefs').select('id,name,gender,city,specialty,bio,rating,available,status,dishes,area').eq('city','casablanca').eq('status','active').eq('membership_status','active').order('created_at',{ascending:false});if(error){grid.innerHTML='<div class="empty-state"><strong>تعذر الاتصال بقاعدة البيانات.</strong><span>سنصلح الاتصال قبل استقبال التسجيلات.</span></div>';return}chefsCache=data||[];if(!chefsCache.length){grid.innerHTML='<div class="empty-state"><strong>لا توجد مطابخ معتمدة بعد.</strong><span>أول الملفات ستظهر هنا فور اعتمادها من Ommi Food.</span><button class="join inline-join" data-open-empty>انضمي إلى Ommi Food</button></div>';grid.querySelector('[data-open-empty]')?.addEventListener('click',()=>openModal(joinModal));return}grid.innerHTML=chefsCache.map(c=>`<article class="chef-card" data-chef="${c.id}"><div class="chef-main"><div class="chef-name">${prefix(c.gender)} ${escapeHtml(c.name)}</div><div class="chef-specialty">${escapeHtml(c.specialty||'')}</div><div class="chef-meta"><span>${escapeHtml(c.area||'الدار البيضاء')}</span>${c.rating?`<span>★ ${Number(c.rating).toFixed(1)}</span>`:''}<span class="status ${c.available?'available':'busy'}">${c.available?'متاحة الآن':'غير متاحة الآن'}</span></div></div><div class="arrow">←</div></article>`).join('');grid.querySelectorAll('[data-chef]').forEach(el=>el.addEventListener('click',()=>openChef(el.dataset.chef)))}
function escapeHtml(v){return String(v??'').replace(/[&<>'\"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[ch]))}function normaliseDishes(raw){if(!raw)return[];if(Array.isArray(raw))return raw.map(d=>Array.isArray(d)?{name:d[0],price:d[1]}:d).filter(d=>d&&d.name);return[]}
function openChef(id){activeChef=chefsCache.find(c=>String(c.id)===String(id));if(!activeChef)return;activeDish=null;document.getElementById('orderChefName').textContent=`${prefix(activeChef.gender)} ${activeChef.name}`;document.getElementById('orderChefArea').textContent=activeChef.area||'الدار البيضاء';const dishes=normaliseDishes(activeChef.dishes),list=document.getElementById('dishList');if(!dishes.length)list.innerHTML='<div class="empty-dishes">هذه الطاهية لم تنشر أطباقها بعد.</div>';else{list.innerHTML=dishes.map((d,i)=>`<button class="dish-option" data-dish="${i}"><span>${escapeHtml(d.name)}</span><strong>${escapeHtml(d.price??'')}</strong></button>`).join('');list.querySelectorAll('[data-dish]').forEach(b=>b.addEventListener('click',()=>{list.querySelectorAll('.dish-option').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');activeDish=dishes[Number(b.dataset.dish)]}))}openModal(orderModal)}
document.getElementById('discoverBtn').addEventListener('click',async()=>{const city=citySelect.value;if(!city){showToast('اختر المدينة أولًا');return}if(city!=='casablanca'){const name=citySelect.options[citySelect.selectedIndex].text.split(' — ')[0];document.getElementById('comingTitle').textContent=`قريبًا في ${name}`;openModal(comingModal);return}document.getElementById('cityTitle').textContent='الدار البيضاء';showScreen('chefs');await loadChefs()});
document.getElementById('submitOrderBtn').addEventListener('click',async()=>{if(!activeChef)return;if(!activeDish){showToast('اختر طبقًا أولًا');return}const name=document.getElementById('customerName').value.trim(),phone=document.getElementById('customerPhone').value.trim(),area=document.getElementById('customerArea').value.trim();if(!name||!phone||!area){showToast('أكمل معلومات الطلب');return}const ref='OF-'+Math.floor(10000+Math.random()*90000),fulfilment=document.querySelector('input[name="fulfilment"]:checked')?.value||'pickup';const{error}=await db.from('orders').insert({order_ref:ref,chef_id:activeChef.id,chef_name:`${prefix(activeChef.gender)} ${activeChef.name}`,dish_name:activeDish.name,customer_name:name,customer_phone:phone,customer_address:area,status:'pending',fulfilment_type:fulfilment});if(error){showToast('تعذر تسجيل الطلب حاليًا');return}closeModal(orderModal);showToast(`تم تسجيل الطلب ${ref}`)});
document.querySelectorAll('.gender').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.gender').forEach(x=>x.classList.remove('active'));b.classList.add('active');joinGender=b.dataset.gender==='male'?'m':'f'}));document.getElementById('joinName').addEventListener('input',e=>{const cleaned=cleanName(e.target.value);if(e.target.value!==cleaned)e.target.value=cleaned});
document.getElementById('submitJoinBtn').addEventListener('click',async()=>{clearJoinErrors();const name=cleanName(document.getElementById('joinName').value),phone=document.getElementById('joinPhone').value.trim(),area=document.getElementById('joinArea').value.trim(),bio=document.getElementById('joinBio').value.trim(),consent=document.getElementById('joinConsent').checked;if(!name){joinFieldError('joinName','أدخلي اسمك الأول.');return}if(!phone){joinFieldError('joinPhone','أدخلي رقم الهاتف.');return}if(!/^0[5-7][0-9]{8}$/.test(phone)){joinFieldError('joinPhone','أدخلي رقم هاتف مغربي صحيحًا من 10 أرقام.');return}if(!area){joinFieldError('joinArea','أدخلي الحي أو المنطقة.');return}if(!consent){document.getElementById('joinConsent').closest('label')?.classList.add('consent-invalid');showToast('وافقي على شروط المنصة للمتابعة');return}const btn=document.getElementById('submitJoinBtn');btn.disabled=true;btn.textContent='جاري الإرسال...';const{error}=await db.from('chefs').insert({name,phone,city:'casablanca',city_label:'الدار البيضاء',area,bio:bio||null,gender:joinGender,status:'pending',available:false,membership_type:'none',membership_status:'inactive'});btn.disabled=false;btn.textContent='إرسال طلب الانضمام';if(error){console.error('Join request failed:',error);showToast(`تعذر إرسال الطلب: ${error.message||'خطأ في قاعدة البيانات'}`);return}document.getElementById('joinName').value='';document.getElementById('joinPhone').value='';document.getElementById('joinArea').value='';document.getElementById('joinBio').value='';document.getElementById('joinConsent').checked=false;clearJoinErrors();closeModal(joinModal);showScreen('joinReceived')});